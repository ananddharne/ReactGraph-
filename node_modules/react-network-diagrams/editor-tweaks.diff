diff --git a/examples/modules/editor_example.jsx b/examples/modules/editor_example.jsx
index 54e8b63..a4754db 100644
--- a/examples/modules/editor_example.jsx
+++ b/examples/modules/editor_example.jsx
@@ -82,8 +82,8 @@ export default React.createClass({
 
     renderEditor() {
         const bounds = {
-            x1: 0, y1: 0,
-            x2: 225, y2: 120
+            x1: -5, y1: 0,
+            x2: 250, y2: 120
         };
 
         // Maps link capacity to line thickness
diff --git a/src/map-editor.jsx b/src/map-editor.jsx
index 0c291de..d65bb94 100644
--- a/src/map-editor.jsx
+++ b/src/map-editor.jsx
@@ -187,6 +187,7 @@ export default React.createClass({
             description: this.props.topology.description,
             nodes: _.map(this.props.topology.nodes, (n) => _.clone(n)),
             edges: _.map(this.props.topology.edges, (e) => _.clone(e)),
+            paths: _.map(this.props.topology.paths, (p) => _.clone(p)),
         };
         return topo;
     },
